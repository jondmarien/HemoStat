# HemoStat Base Infrastructure Dependencies
# ============================================================================
# NOTE: This project uses UV for Python package management
# See pyproject.toml for the canonical dependency definitions
# ============================================================================
#
# UV Installation:
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   or: pip install uv
#
# Usage:
#   uv sync                    # Install dependencies from pyproject.toml
#   uv run python script.py    # Run Python scripts
#   uv pip install package     # Install additional packages
#   uv venv                    # Create virtual environment
#
# ============================================================================
# Phase 1: Core Infrastructure (Current)
# ============================================================================
redis==5.0.1              # Redis Python client with pub/sub support
python-dotenv==1.0.0      # Environment variable loading from .env files
python-json-logger==2.0.7 # Structured JSON logging

# ============================================================================
# Phase 2: Agent Implementations (Optional)
# Install with: uv sync --extra agents
# ============================================================================
# docker==7.0.0                    # Monitor/Responder agents for container management
# langchain==0.1.0                 # LLM orchestration for Analyzer agent
# openai==1.10.0                   # OpenAI GPT-4 integration
# anthropic==0.8.0                 # Anthropic Claude integration
# requests==2.31.0                 # HTTP client for Alert agent (Slack webhooks)

# ============================================================================
# Phase 3: Dashboard (Optional)
# Install with: uv sync --extra dashboard
# ============================================================================
# streamlit==1.29.0                # Web UI framework for live monitoring dashboard

# ============================================================================
# Phase 4: Testing & Development (Optional)
# Install with: uv sync --extra dev
# ============================================================================
# pytest==7.4.0                    # Unit testing framework
# pytest-asyncio==0.21.0           # Async test support
# pytest-cov==4.1.0                # Code coverage reporting

# ============================================================================
# Install all optional dependencies with: uv sync --extra all
# ============================================================================
