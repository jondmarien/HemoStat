# Docker Compose overrides for Linux
# Use with: docker compose -f docker-compose.yml -f docker-compose.linux.yml up -d

services:
  monitor:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  responder:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
